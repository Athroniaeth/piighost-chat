[project]
name = "piighost-chat"
version = "0.1.0"
description = "Chat interface with PII anonymization, powered by piighost-api and LangChain."
readme = "README.md"
license = { text = "MIT" }
authors = [
    { name = "Athroniaeth" },
]
requires-python = ">=3.12"
dependencies = [
    "piighost[client,middleware]>=1.0",
    "langchain>=1.2",
    "langgraph-checkpoint-postgres>=2.0",
    "litestar>=2.16",
    "uvicorn>=0.34",
    "msgspec>=0.19",
    "psycopg[binary]>=3.2",
    "keyshield[cli]>=2.0.0",
    "taskiq>=0.11",
    "taskiq-redis>=1.0",
    "langchain-litellm>=0.6.4",
    "langfuse>=3.0",
]

[tool.uv.sources]
# Local-dev override: when the sibling ../../piighost checkout exists,
# uv installs piighost editable from there. Production / CI / Docker
# builds pass `--no-sources` to ignore this and pull piighost from PyPI.
piighost = { path = "../../piighost", editable = true }

[project.scripts]
piighost-chat = "piighost_chat.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/piighost_chat"]

[tool.pyrefly]
search_path = ["src"]

[tool.pytest.ini_options]
addopts = "--cov=src/piighost_chat"
asyncio_mode = "auto"

[tool.coverage.report]
show_missing = true

[dependency-groups]
dev = [
    "ruff>=0.15",
    "pytest>=9.0",
    "pytest-cov>=6.0",
    "pytest-asyncio>=0.25",
    "pyrefly>=0.55",
]
